package user.subscription;

/**
 * Defines the available subscription types in the MiniSpotify system.
 * Used to categorize different subscription plans with varying features and pricing.
 */
public enum SubscriptionType {
    FREE,
    PREMIUM,
    FAMILY,
    STUDENT
}
